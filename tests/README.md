# Cross-stack tests

Place end-to-end tests that span the desktop and quant engine here. Unit and service integration tests stay beside their owning package under `apps/desktop/tests` and `services/quant-engine/tests`.

Fixtures must be deterministic and sanitized. Never add real platform screenshots, browser/session data, or local market datasets.
